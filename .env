# Valores por defecto (sitio real en Hostinger).
VITE_ROBOTS=index, follow
# Archivo PHP que envía el formulario por correo (está en public/contacto.php).
VITE_FORM_ENDPOINT=contacto.php
# true = el formulario solo muestra la confirmación, sin enviar nada (vista previa en GitHub Pages).
VITE_FORM_DEMO=false
